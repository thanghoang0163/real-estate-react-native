'use strict';

/**
 * outstanding-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::outstanding-project.outstanding-project');
